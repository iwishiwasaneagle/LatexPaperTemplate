% \begin{tikzpicture}[
%         node distance=1cm
%     ]
%     \node[ml/node] (sum) {$\sum$};
%     \node[ml/node, left=of sum] (x1) {$x_1$};
%     \node[ml/node, above=of x1] (x0) {$x_0$};
%     \node[ml/node, below=of x1] (xn) {$x_n$};
%     \node[ml/node] (bias) at (sum |- x0) {$b$};
%     \node[right=of sum] (activation_fn) {$f \left(b+\sum\limits^n_{i=0} w_i x_i \right)$};

%     \path (x1) -- node[midway] {$\vdots$} (xn);

%     \draw[ml/weights] (bias) -- (sum);
%     \draw[ml/weights] (x0) -- node{\contour{white}{$w_0$}} (sum);
%     \draw[ml/weights] (x1) -- node{\contour{white}{$w_1$}} (sum);
%     \draw[ml/weights] (xn) -- node{\contour{white}{$w_n$}} (sum);

%     \draw[ml/weights] (sum) --  (activation_fn);
% \end{tikzpicture}

\begin{tikzpicture}[
        node distance=1cm
    ]
    \node[ml/node,] (x) {$x_t$};
    \node[operator, right=of x] (hadmard1) {$\cdot$};
    \node[gt, above=of hadmard1] (w) {$W$};
    \node[operator, right=of hadmard1] (sum1) {+};
    \node[gt, below=of sum1] (bias) {$b$};

    \node[function, right=of sum1] (activation_fn) {$f(\cdot)$};
    \node[ml/node,right=of activation_fn] (y) {$y_t$};

    \node[cell, fit=(bias) (w) (activation_fn) (sum1) (hadmard1), rounded corners=0.25cm, inner sep=8pt] {};

    \draw[ml/weights/rounded1] (x) -- (hadmard1);
    \draw[ml/weights/rounded1] (w) -- (hadmard1);
    \draw[->, ml/weights/rounded1] (hadmard1) -- (sum1);
    \draw[->, ml/weights/rounded1] (bias) -- (sum1);
    \draw[->, ml/weights/rounded1] (sum1) --  (activation_fn);
    \draw[ml/weights/rounded1] (activation_fn) --  (y);
\end{tikzpicture}